apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    version: 1
    access: proxy
    orgId: 1
    basicAuth: false
    editable: false
    url: http://{{ hostvars[groups['prometheus_servers'][0]]['ansible_default_ipv4']['address'] }}:9090